const moogosh = require("mongoose")
const Schema = moogosh.Schema

const newBanner = new Schema({
    tittle: {
        type: String
    },
    externallink: {
        type: String
    },
    isnewPgeL: {
        type: Boolean
    },
    headerText: {
        type: String
    }, 
 otherstext: {
        type: String,
    }
})